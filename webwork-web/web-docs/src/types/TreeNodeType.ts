/**
 * 树节点类型定义
 */
export const enum TreeNodeType {
    // 组织架构中的虚拟节点
    VIRTUAL = 0,
    // 单位
    GROUP = 1,
    // 部门
    DEPT = 2,
    // 用户
    USER = 3,
    // 角色
    ROLE = 10,
    // 应用
    APP = 20,
    // 模块
    MODULE = 21,
    // 功能
    FUNCTION = 22
}
